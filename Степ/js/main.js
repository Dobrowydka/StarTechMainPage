// Модальное окно1

//1 картинка
// открыть по кнопке
$('.js-button-campaign1').click(function () {
//	$('.portfolio-section, .prev_section').css('filter','blur(5px)');
	
    $('.js-overlay-campaign1').fadeIn();
    $('.js-overlay-campaign1').addClass('disabled');
});

// закрыть на крестик
$('.js-close-campaign1').click(function () {
    $('.js-overlay-campaign1').fadeOut();
//	$('.portfolio-section, .prev_section').css('filter','blur(0px)');
});

// закрыть по клику вне окна
$(document).mouseup(function (e) {
    var popup = $('.js-popup-campaign1');
    if (e.target != popup[0] && popup.has(e.target).length === 0) {
        $('.js-overlay-campaign1').fadeOut();
//		$('.portfolio-section, .prev_section').css('filter','blur(0px)');
    }
});

// Модальное окно 2

//1 картинка
// открыть по кнопке
$('.js-button-campaign4').click(function () {
    //	$('.portfolio-section, .prev_section').css('filter','blur(5px)');
        
        $('.js-overlay-campaign4').fadeIn();
        $('.js-overlay-campaign4').addClass('disabled');
    });
    
    // закрыть на крестик
    $('.js-close-campaign4').click(function () {
        $('.js-overlay-campaign4').fadeOut();
    //	$('.portfolio-section, .prev_section').css('filter','blur(0px)');
    });
    
    // закрыть по клику вне окна
    $(document).mouseup(function (e) {
        var popup = $('.js-popup-campaign4');
        if (e.target != popup[0] && popup.has(e.target).length === 0) {
            $('.js-overlay-campaign4').fadeOut();
    //		$('.portfolio-section, .prev_section').css('filter','blur(0px)');
        }
    });
// Модальное окно 3

//1 картинка
// открыть по кнопке
$('.js-button-campaign5').click(function () {
    //	$('.portfolio-section, .prev_section').css('filter','blur(5px)');
        
        $('.js-overlay-campaign5').fadeIn();
        $('.js-overlay-campaign5').addClass('disabled');
    });
    
    // закрыть на крестик
    $('.js-close-campaign5').click(function () {
        $('.js-overlay-campaign5').fadeOut();
    //	$('.portfolio-section, .prev_section').css('filter','blur(0px)');
    });
    
    // закрыть по клику вне окна
    $(document).mouseup(function (e) {
        var popup = $('.js-popup-campaign5');
        if (e.target != popup[0] && popup.has(e.target).length === 0) {
            $('.js-overlay-campaign5').fadeOut();
    //		$('.portfolio-section, .prev_section').css('filter','blur(0px)');
        }
    });
// Модальное окно 4

//1 картинка
// открыть по кнопке
$('.js-button-campaign8').click(function () {
    //	$('.portfolio-section, .prev_section').css('filter','blur(5px)');
        
        $('.js-overlay-campaign8').fadeIn();
        $('.js-overlay-campaign8').addClass('disabled');
    });
    
    // закрыть на крестик
    $('.js-close-campaign8').click(function () {
        $('.js-overlay-campaign8').fadeOut();
    //	$('.portfolio-section, .prev_section').css('filter','blur(0px)');
    });
    
    // закрыть по клику вне окна
    $(document).mouseup(function (e) {
        var popup = $('.js-popup-campaign8');
        if (e.target != popup[0] && popup.has(e.target).length === 0) {
            $('.js-overlay-campaign8').fadeOut();
    //		$('.portfolio-section, .prev_section').css('filter','blur(0px)');
        }
    });

    // Модальное окно 5

//1 картинка
// открыть по кнопке
$('.js-button-campaign2').click(function () {
    //	$('.portfolio-section, .prev_section').css('filter','blur(5px)');
        
        $('.js-overlay-campaign2').fadeIn();
        $('.js-overlay-campaign2').addClass('disabled');
    });
    
    // закрыть на крестик
    $('.js-close-campaign2').click(function () {
        $('.js-overlay-campaign2').fadeOut();
    //	$('.portfolio-section, .prev_section').css('filter','blur(0px)');
    });
    
    // закрыть по клику вне окна
    $(document).mouseup(function (e) {
        var popup = $('.js-popup-campaign2');
        if (e.target != popup[0] && popup.has(e.target).length === 0) {
            $('.js-overlay-campaign2').fadeOut();
    //		$('.portfolio-section, .prev_section').css('filter','blur(0px)');
        }
    });
       // Модальное окно 5

//1 картинка
// открыть по кнопке
$('.js-button-campaign22').click(function () {
    //	$('.portfolio-section, .prev_section').css('filter','blur(5px)');
        
        $('.js-overlay-campaign22').fadeIn();
        $('.js-overlay-campaign22').addClass('disabled');
    });
    
    // закрыть на крестик
    $('.js-close-campaign22').click(function () {
        $('.js-overlay-campaign22').fadeOut();
    //	$('.portfolio-section, .prev_section').css('filter','blur(0px)');
    });
    
    // закрыть по клику вне окна
    $(document).mouseup(function (e) {
        var popup = $('.js-popup-campaign22');
        if (e.target != popup[0] && popup.has(e.target).length === 0) {
            $('.js-overlay-campaign22').fadeOut();
    //		$('.portfolio-section, .prev_section').css('filter','blur(0px)');
        }
    });
       // Модальное окно 5

//1 картинка
// открыть по кнопке
$('.js-button-campaign23').click(function () {
    //	$('.portfolio-section, .prev_section').css('filter','blur(5px)');
        
        $('.js-overlay-campaign23').fadeIn();
        $('.js-overlay-campaign23').addClass('disabled');
    });
    
    // закрыть на крестик
    $('.js-close-campaign23').click(function () {
        $('.js-overlay-campaign23').fadeOut();
    //	$('.portfolio-section, .prev_section').css('filter','blur(0px)');
    });
    
    // закрыть по клику вне окна
    $(document).mouseup(function (e) {
        var popup = $('.js-popup-campaign23');
        if (e.target != popup[0] && popup.has(e.target).length === 0) {
            $('.js-overlay-campaign23').fadeOut();
    //		$('.portfolio-section, .prev_section').css('filter','blur(0px)');
        }
    });
           // Модальное окно 6

//1 картинка
// открыть по кнопке
$('.js-button-campaign3').click(function () {
    //	$('.portfolio-section, .prev_section').css('filter','blur(5px)');
        
        $('.js-overlay-campaign3').fadeIn();
        $('.js-overlay-campaign3').addClass('disabled');
    });
    
    // закрыть на крестик
    $('.js-close-campaign3').click(function () {
        $('.js-overlay-campaign3').fadeOut();
    //	$('.portfolio-section, .prev_section').css('filter','blur(0px)');
    });
    
    // закрыть по клику вне окна
    $(document).mouseup(function (e) {
        var popup = $('.js-popup-campaign3');
        if (e.target != popup[0] && popup.has(e.target).length === 0) {
            $('.js-overlay-campaign3').fadeOut();
    //		$('.portfolio-section, .prev_section').css('filter','blur(0px)');
        }
    });
               // Модальное окно 6

//1 картинка
// открыть по кнопке
$('.js-button-campaign32').click(function () {
    //	$('.portfolio-section, .prev_section').css('filter','blur(5px)');
        
        $('.js-overlay-campaign32').fadeIn();
        $('.js-overlay-campaign32').addClass('disabled');
    });
    
    // закрыть на крестик
    $('.js-close-campaign32').click(function () {
        $('.js-overlay-campaign32').fadeOut();
    //	$('.portfolio-section, .prev_section').css('filter','blur(0px)');
    });
    
    // закрыть по клику вне окна
    $(document).mouseup(function (e) {
        var popup = $('.js-popup-campaign32');
        if (e.target != popup[0] && popup.has(e.target).length === 0) {
            $('.js-overlay-campaign32').fadeOut();
    //		$('.portfolio-section, .prev_section').css('filter','blur(0px)');
        }
    });
              // Модальное окно 7

//1 картинка
// открыть по кнопке
$('.js-button-campaign6').click(function () {
    //	$('.portfolio-section, .prev_section').css('filter','blur(5px)');
        
        $('.js-overlay-campaign6').fadeIn();
        $('.js-overlay-campaign6').addClass('disabled');
    });
    
    // закрыть на крестик
    $('.js-close-campaign6').click(function () {
        $('.js-overlay-campaign6').fadeOut();
    //	$('.portfolio-section, .prev_section').css('filter','blur(0px)');
    });
    
    // закрыть по клику вне окна
    $(document).mouseup(function (e) {
        var popup = $('.js-popup-campaign6');
        if (e.target != popup[0] && popup.has(e.target).length === 0) {
            $('.js-overlay-campaign6').fadeOut();
    //		$('.portfolio-section, .prev_section').css('filter','blur(0px)');
        }
    });
    // Модальное окно 7

//1 картинка
// открыть по кнопке
$('.js-button-campaign62').click(function () {
    //	$('.portfolio-section, .prev_section').css('filter','blur(5px)');
        
        $('.js-overlay-campaign62').fadeIn();
        $('.js-overlay-campaign62').addClass('disabled');
    });
    
    // закрыть на крестик
    $('.js-close-campaign62').click(function () {
        $('.js-overlay-campaign62').fadeOut();
    //	$('.portfolio-section, .prev_section').css('filter','blur(0px)');
    });
    
    // закрыть по клику вне окна
    $(document).mouseup(function (e) {
        var popup = $('.js-popup-campaign62');
        if (e.target != popup[0] && popup.has(e.target).length === 0) {
            $('.js-overlay-campaign62').fadeOut();
    //		$('.portfolio-section, .prev_section').css('filter','blur(0px)');
        }
    });
     // Модальное окно 8

//1 картинка
// открыть по кнопке
$('.js-button-campaign7').click(function () {
    //	$('.portfolio-section, .prev_section').css('filter','blur(5px)');
        
        $('.js-overlay-campaign7').fadeIn();
        $('.js-overlay-campaign7').addClass('disabled');
    });
    
    // закрыть на крестик
    $('.js-close-campaign7').click(function () {
        $('.js-overlay-campaign7').fadeOut();
    //	$('.portfolio-section, .prev_section').css('filter','blur(0px)');
    });
    
    // закрыть по клику вне окна
    $(document).mouseup(function (e) {
        var popup = $('.js-popup-campaign7');
        if (e.target != popup[0] && popup.has(e.target).length === 0) {
            $('.js-overlay-campaign7').fadeOut();
    //		$('.portfolio-section, .prev_section').css('filter','blur(0px)');
        }
    });
        // Модальное окно 8

//1 картинка
// открыть по кнопке
$('.js-button-campaign72').click(function () {
    //	$('.portfolio-section, .prev_section').css('filter','blur(5px)');
        
        $('.js-overlay-campaign72').fadeIn();
        $('.js-overlay-campaign72').addClass('disabled');
    });
    
    // закрыть на крестик
    $('.js-close-campaign72').click(function () {
        $('.js-overlay-campaign72').fadeOut();
    //	$('.portfolio-section, .prev_section').css('filter','blur(0px)');
    });
    
    // закрыть по клику вне окна
    $(document).mouseup(function (e) {
        var popup = $('.js-popup-campaign72');
        if (e.target != popup[0] && popup.has(e.target).length === 0) {
            $('.js-overlay-campaign72').fadeOut();
    //		$('.portfolio-section, .prev_section').css('filter','blur(0px)');
        }
    });
     // Модальное окно 9

//1 картинка
// открыть по кнопке
$('.js-button-campaign9').click(function () {
    //	$('.portfolio-section, .prev_section').css('filter','blur(5px)');
        
        $('.js-overlay-campaign9').fadeIn();
        $('.js-overlay-campaign9').addClass('disabled');
    });
    
    // закрыть на крестик
    $('.js-close-campaign9').click(function () {
        $('.js-overlay-campaign9').fadeOut();
    //	$('.portfolio-section, .prev_section').css('filter','blur(0px)');
    });
    
    // закрыть по клику вне окна
    $(document).mouseup(function (e) {
        var popup = $('.js-popup-campaign9');
        if (e.target != popup[0] && popup.has(e.target).length === 0) {
            $('.js-overlay-campaign9').fadeOut();
    //		$('.portfolio-section, .prev_section').css('filter','blur(0px)');
        }
    });
    // Модальное окно 9

//1 картинка
// открыть по кнопке
$('.js-button-campaign92').click(function () {
    //	$('.portfolio-section, .prev_section').css('filter','blur(5px)');
        
        $('.js-overlay-campaign92').fadeIn();
        $('.js-overlay-campaign92').addClass('disabled');
    });
    
    // закрыть на крестик
    $('.js-close-campaign92').click(function () {
        $('.js-overlay-campaign92').fadeOut();
    //	$('.portfolio-section, .prev_section').css('filter','blur(0px)');
    });
    
    // закрыть по клику вне окна
    $(document).mouseup(function (e) {
        var popup = $('.js-popup-campaign92');
        if (e.target != popup[0] && popup.has(e.target).length === 0) {
            $('.js-overlay-campaign92').fadeOut();
    //		$('.portfolio-section, .prev_section').css('filter','blur(0px)');
        }
    });